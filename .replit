# Replit configuration
run = "cd backend && uvicorn api.main:app --host 0.0.0.0 --port 8000"
entrypoint = "backend/api/main.py"

[nix]
channel = "stable-23_11"

[deployment]
run = ["sh", "-c", "cd backend && uvicorn api.main:app --host 0.0.0.0 --port 8000"]
deploymentTarget = "cloudrun"

[[ports]]
localPort = 8000
externalPort = 80
